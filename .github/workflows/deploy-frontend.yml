# Frontend is deployed automatically by Cloudflare Pages via git integration.
# Cloudflare Pages detects pushes to main, runs:
#   npm run build && npm run pages:build
# and deploys .vercel/output/static using the wrangler.toml config.
#
# This GitHub Actions workflow is intentionally disabled to avoid conflicts.
# No action needed here.
